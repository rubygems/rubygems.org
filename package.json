{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@github/webauthn-json": "^2.1.1",
    "@rails/ujs": "^7.0.4-2",
    "clipboard": "^2.0.11",
    "esbuild": "^0.17.9",
    "jquery": "^3.6.3",
    "jquery-ujs": "^1.2.3",
    "sass": "^1.58.3"
  },
  "scripts": {
    "_comment": "TODO: minify & terse in prod/staging",
    "build:css": "sass ./app/assets/stylesheets/:./app/assets/builds/ --no-source-map --load-path=node_modules --load-path=vendor/assets/stylesheets",
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds --public-path=assets"
  }
}
