<% @title = "all versions of #{@rubygem.name}" %>
<p>
  <%= @versions.size %> versions since <%= @rubygem.first_built_date %>
</p>
<div class="versions">
  <ol>
    <%= render @versions %>
  </ol>
</div>
