<div class="grid_3" id="new_gems">
  <strong><%= t '.leaderboards.new' %></strong>
  <ol>
    <% @latest.each do |g| %>
      <li><%= link_to gem, gem, title: short_info(gem.versions.most_recent) %></li>
    <% end %>
  </ol>
</div>
