<% content_for :fold do %>
  <div class="count">
    <strong><%= number_with_delimiter(@count) %> gems</strong>
    cut since july 2009
  </div>
  <div class="blurb">
    <strong>Welcome to your community RubyGem host.</strong>
    <br />Find your gems easier, publish them faster, and have fun doing it.
  </div>
<% end %>

<div class="border">
  <div class="background">
    <div class="learn">
      <h3>learn</h3>
      <dl>
        <dt><%= link_to "Install RubyGems 1.3.5", "http://rubyforge.org/projects/rubygems" %></dt>
        <dd>Ruby's premier packaging system</dd>
        <dt><%= link_to "Browse the Docs", "http://docs.rubygems.org" %></dt>
        <dd>The comprehensive guide on RubyGems</dd>
        <dt><%= link_to "Gem Specification", "http://docs.rubygems.org/read/chapter/20" %></dt>
        <dd>Your gem's interface to the world</dd>
      </dl>
    </div>
    <div class="share">
      <h3>share</h3>
      <dl>
        <dt><code>gem update --system</code></dt>
        <dd>Make sure you're up to date</dd>
        <dt><code>gem build foo.gemspec</code></dt>
        <dd>Create your gemspec</dd>
        <dt><code>gem push foo-1.0.0.gem</code></dt>
        <dd>Deploy your gem instantly</dd>
      </dl>
    </div>
  </div>
  <!--
  <ol class="instructions">
    <li id="ready"><strong>ready</strong><span>gem install gemcutter</span></li>
    <li id="set"><strong>set</strong><span>gem tumble</span></li>
    <li id="go"><strong>go</strong><span>gem push yourgem-0.0.1.gem</span></li>
  </ol>
  -->
</div>

<p class="pitch">
  Gemcutter is the <strong>next generation</strong> of gem hosting for the Ruby community.
  <strong>Instantly publish</strong> your gems and install them.
  <strong>Use the API</strong> to interact and find out more information about available gems.
  <strong>Become a contributor</strong> and enhance the site with your own changes.
</p>
<div class="leaderboards">
  <div class="grid_3" id="new_gems">
    <strong>New Gems</strong>
    <ol>
      <% @latest.each do |rubygem| %>
        <li>
          <%= link_to rubygem, rubygem, :title => rubygem.versions.latest.summary %>
        </li>
      <% end %>
    </ol>
  </div>
  <div class="grid_3" id="most_downloaded">
    <strong>Most Downloaded</strong>
    <ol>
      <% @downloaded.each do |rubygem| %>
        <li>
          <%= link_to "#{rubygem.name} (#{rubygem.downloads})", rubygem, :title => rubygem.versions.latest.summary %>
        </li>
      <% end %>
    </ol>
  </div>
  <div class="grid_3" id="just_updated">
    <strong>Just Updated</strong>
    <ol>
      <% @updated.each do |version| %>
        <li>
          <%= link_to version.to_title, version.rubygem, :title => version.summary %>
        </li>
      <% end %>
    </ol>
  </div>
</div>
