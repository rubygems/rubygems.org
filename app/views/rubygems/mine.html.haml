-@title = "my gems"
-@subtitle = ""
.gems.grid_12
  -if @gems.empty?
    %h3 Create
    =render 'new'
    %h3 Migrate
    =render 'migrate'
  -else
    -@gems.each do |gem|
      =link_to gem, rubygem_path(gem)
