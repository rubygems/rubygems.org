<% @title = t('.title') %>

<div class="gems__nav-links">
  <%= link_to_directory %>
</div>

<header class="gems__header">
  <p class="gems__meter">
    <%= page_entries_info @gems, :entry_name => 'gem'%>
  </p>
</header>

<%= render @gems %>

<%= paginate @gems %>
