<a class="gems__gem" href="<%= rubygem_path(rubygem) %>">
  <div class="gems__gem__info">
    <h2 class="gems__gem__name">
      <%= rubygem.name %>
      <span class="gems__gem__version"><%= latest_version_number(rubygem) %></span>
    </h2>
    <p class="gems__gem__desc t-text"><%= emojify short_info(rubygem.versions.most_recent) %></p>
  </div>
  <p class="gems__gem__downloads__count">
    <%= download_count rubygem %>
    <span class="gems__gem__downloads__heading">Downloads</span>
  </p>
</a>
